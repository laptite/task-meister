%div.col-md-10.cd-md-offset-1
	%h2= @item.title
	%p= @item.description

	%div.col-md-4.col-md-offset-4
		= link_to "Back", root_path
		= link_to "Edit", edit_item_path(@item)
		= link_to "Delete", item_path(@item), method: :delete, data: { confirm: "Are you sure you want to proceed?"}